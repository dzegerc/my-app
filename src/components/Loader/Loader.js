import Loader from 'react-loader-spinner';

const LoaderStyle = () => {

    return (
        <Loader
    type="Oval"
    color="#e4b43c"
    height={80}
    widt={80}
    timeout={1000}
    />
    );
}

export default LoaderStyle